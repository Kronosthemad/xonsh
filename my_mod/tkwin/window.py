import tkinter as tk
import collections
import os
import path


class windowBuilder(self,[]):
    def __init__(self):
        super.__init__(self)
        self.window = tk.Tk()
        self.widget = tk.Widget(self.window, "Command window")
        self.label = tk.Label(self.widget, {})
        self.button = tk.Button(self.window, {})

    def default(self):
        self.widget.config(geometry="400x500")
        self.label.config(text="hello")
        self.button.config(text="clickme", Command(exit(2)))

    def get_from_readline(collections.issubclass):
        os.O_ASYNC():
